<h1><?= htmlspecialchars($group['name'], ENT_QUOTES, 'UTF-8') ?></h1>

<section>
    <h2>Membres</h2>
    <ul class="member-list">
        <?php foreach ($members as $member): ?>
            <li>
                <?= htmlspecialchars($member['username'], ENT_QUOTES, 'UTF-8') ?>
                <?php if ($member['id'] == $group['created_by']): ?>
                    <span class="admin-badge">Admin</span>
                <?php endif; ?>
            </li>
        <?php endforeach; ?>
    </ul>
    
    <h2>Balances</h2>
    <table>
        <thead>
            <tr>
                <th>Membre</th>
                <th>Balance</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($balances as $userId => $balance): ?>
                <tr>
                    <td><?= htmlspecialchars($balances[$userId]['username'], ENT_QUOTES, 'UTF-8') ?></td>
                    <td class="<?= $balance['amount'] < 0 ? 'negative' : 'positive' ?>">
                        <?= number_format($balance['amount'], 2) ?> €
                    </td>
                </tr>
            <?php endforeach; ?>